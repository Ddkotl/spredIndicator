import { getSpotSymbols, getFuturesSymbols } from "./api/symbols";
import { buildUniverse } from "./engine/universe";
import { connectBatch } from "./ws/batch";
import { onSpot, onFutures } from "./engine/scanner";

async function main() {
  console.log("Loading universe...");

  const spot = await getSpotSymbols();
  const fut = await getFuturesSymbols();

  const universe = buildUniverse(spot, fut).slice(0, 200);

  console.log("Pairs:", universe.length);

  const chunkSize = 20;
  const chunks: string[][] = [];

  for (let i = 0; i < universe.length; i += chunkSize) {
    chunks.push(universe.slice(i, i + chunkSize));
  }

  for (const chunk of chunks) {
    connectBatch("spot", chunk, onSpot);
    connectBatch("futures", chunk, onFutures);
  }
}

main();

// Привет! Действуй как Senior Solution Architect и Lead Backend Engineer. Твоя задача — провести глубокий аудит исходного кода моего проекта и составить документацию, разделенную строго на два независимых файла. 

// Проанализируй структуру каталогов, конфигурации, модели данных, архитектурные слои, паттерны и бизнес-логику, которые я тебе передам. 

// Сформируй ответ строго в виде двух блоков/файлов:

// ### ФАЙЛ №1: Архитектурный паспорт, структура пакетов и системная логика (Technical Design Document)

// 1. Архитектурный паттерн: Определи и классифицируй текущую архитектуру (например, Clean Architecture, DDD, слоистая/Layered, Монолит, Микросервисы). Объясни, как слои взаимодействуют друг с другом.
// 2. Карта пакетов и модулей: Разбери дерево каталогов проекта. Опиши зону ответственности (Responsibility) каждого ключевого пакета, модуля и папки.
// 3. Анализ потока данных (Data Flow & Request Lifecycle): Пошагово опиши путь прохождения запроса/данных — от точки входа (API Controller / Route Handler / Event Listener) через слой бизнес-логики (Services / Use Cases) до слоя данных (Repositories / ORM / DB) и обратно.
// 4. Ключевые механизмы и логика: Детально разбери логику основных сервисов, алгоритмы обработки данных, интеграции с внешними API, очереди сообщений и конфигурации, которые присутствуют в коде.

// ### ФАЙЛ №2: Технический долг, аудит кода и дорожная карта развития (Code Audit & Engineering Roadmap)

// 1. Архитектурные недостатки и антипаттерны: Укажи на нарушения принципов SOLID, DRY, KISS, YAGNI. Найди места со слишком сильной связанностью (Tight Coupling), "божественными объектами" (God Objects) или размытой зоной ответственности.
// 2. Узкие места производительности (Bottlenecks) и безопасность: Выяви потенциальные проблемы с производительностью (N+1 запросы к БД, отсутствие индексов, утечки памяти, неоптимальные циклы) и уязвимости в безопасности кода.
// 3. Тестируемость системы (Testability): Оцени текущее тестовое покрытие (если оно есть) или опиши, насколько легко данный код поддается Unit, Integration и End-to-End тестированию. Выдели критические зоны, требующие тестов в первую очередь.
// 4. Дорожная карта развития (Roadmap): Составь конкретный, приоритезированный план действий, разбитый на 3 этапа:
//    - Краткосрочный (Quick Wins): что исправить немедленно (Hotfixes, простой рефакторинг).
//    - Среднесрочный: внедрение кэширования, оптимизация запросов, улучшение структуры папок, логирование.
//    - Долгосрочный (Scalability): шаги по масштабированию, CI/CD, переход на микросервисы или внедрение очередей (если применимо).

// Пиши максимально конкретно, техническим языком с пояснениями для  новичка, подкрепляя выводы ссылками на названия конкретных классов, функций, методов и файлов из моего кода. Избегай общих фраз.